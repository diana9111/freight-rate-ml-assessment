Place the assessment-provided CSV files in this directory locally.

The `.gitignore` intentionally excludes the CSVs from GitHub so the repository contains code and reproducibility instructions without committing the assessment data.
